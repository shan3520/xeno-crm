CRM_RECEIPT_URL=http://localhost:3001/receipts
DELIVERED_RATE=0.92
OPEN_RATE=0.55
CLICK_RATE=0.30
CONVERT_RATE=0.15
DUPLICATE_PCT=0.05
MIN_DELAY_MS=500
MAX_DELAY_MS=30000

# Optional: shared HMAC secret matching crm-api's CALLBACK_HMAC_SECRET. When set, each receipt POST
# is signed with an x-signature header. EMPTY (default) = unsigned, backward-compatible callbacks.
# CALLBACK_HMAC_SECRET=
